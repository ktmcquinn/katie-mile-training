{
  "name": "dresden-half",
  "version": "1.0.0",
  "private": true,
  "description": "The Dresden Half — personal half-marathon + mile training PWA.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
